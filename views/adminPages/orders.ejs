<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="scss/_utilities.scss.">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <!-- <button class="btn btn-danger" onclick="window.location.href=''">ADD</button> <br> -->
    <table class="table" style="margin-top: 1rem;">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>MEAL ORDERED</th>
                <th>STUDENT FIRSTNAME</th>
                <th>STUDENT LASTNAME</th>
                <th>LOCATION</th>
                <th>EMAIL</th>
                <th>ACTION</th>
            </tr>
        </thead>
        <tbody>
            <% if(data.length > 0) { %>
                <% for (let i = 0; i < data.length; i++) { %>
                    <tr>
                        <td> <%= data[i].id %></td>
                        <td> <%= data[i].meal_ordered %></td>
                        <td> <%= data[i].stud_fn %></td>
                        <td> <%= data[i].stud_ln %></td>
                        <td> <%= data[i].location %></td>
                        <td> <%= data[i].email %></td>
                        <td>
                            <button class="btn btn-danger"  onclick="window.location.href='/deleteOrder/<%= data[i].id %>'">CANCEL ORDER</button>
                          
                            
                        </td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="5">No data available</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    
</body>
</html>