<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="scss/_utilities.scss.">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <button class="btn btn-danger"  onclick="window.location.href='/mealsInputs'">ADD</button> <br>
    <table class="table" style="margin-top: 1rem;">
        <thead class="table-dark">
            <tr>
                <th style="text-align:left;">ID</th>
                <th colspan="2" style="text-align:center;">MEAL</th>
                <th colspan="2" style="text-align:center;">IMAGES OF MEAL</th>
                <th colspan="2" style="text-align: center;">ACTION</th>
            </tr>
        </thead>
    
        <tbody>
            <% if(data.length > 0) { %> 
                <% for (let i = 0; i < data.length; i++) { %>
                    <tr>
                        <td style="text-align:left;"><%= data[i].id %></td>
                        <td colspan="2" style="text-align:center;"><%= data[i].meal_name %></td>
                        <td colspan="2" style="text-align:center;"><%= data[i].image %></td>
                        <td colspan="1" style="text-align: center;">
                           <button class="btn btn-danger"  onclick="window.location.href='/contactInputs'">EDIT</button>
                           <button class="btn btn-danger"  onclick="window.location.href='/deleteMeal/<%= data[i].id %> '">DELETE</button>
                        </td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="5" style="text-align:center;">No data available</td>
                </tr>
            <% } %>
        </tbody>   
    </table>

    <nav aria-label="Page navigation" class="mt-5 ml-5" style="margin-left: 4rem;">
        <ul class="pagination">
            <li class="page-item">
                <a class="page-link" href="/adminPage?page=1" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous Day</span>
                </a>
            </li>
         
            <li class="page-item">
                <a class="page-link" href="/adminPage?page=2" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next Day</span>
                </a>
            </li>
        </ul>
    </nav>

    
    
    
    
</body>
</html>