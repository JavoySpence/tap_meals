<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacts List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="scss/_utilities.scss.">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <button class="btn btn-danger"  onclick="window.location.href='/contactInputs'">Danger</button> <br>

    <table table class="table" style="margin-top: 1rem;">
        <thead class="table-dark">
            <tr>
                <th>STUDENT NAME</th>
                <th>LOCATION</th>
                <th>ADDRESS</th>
                <th>ACTION</th>
            </tr>
        </thead>

        <tbody>
            <% if (data.length > 0) { %>
                <% for (let i = 0; i < data.length; i++) { %>
                    <tr>
                        <td><%= data[i].id %></td>
                        <td><%= data[i].location %></td>
                        <td><%= data[i].address %></td>
                        <td>
                            <button class="btn btn-danger"  onclick="window.location.href='/editContact/<%= data[i].id %>'">Edit</button>
                            <button class="btn btn-danger"  onclick="window.location.href='/viewContact/<%= data[i].id %> '">View</button>
                            <button class="btn btn-danger"  onclick="window.location.href='/delete/<%= data[i].id %>'">Delete</button>
                            
                          
                        </td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="3">No data available</td>
                </tr>
            <% } %>
        </tbody>
        
    </table>
</body>
</html>